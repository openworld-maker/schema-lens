# Legacy Helm Path (Compatibility)

This chart path is retained for backward compatibility.

Primary chart path is now:

- `helm/solrguard`

Deprecation schedule:

- legacy path support planned through `v0.6.x`
- removal planned in a future major release after migration notices

See:

- `docs/deprecation-schedule.md`
