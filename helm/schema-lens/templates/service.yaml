apiVersion: v1
kind: Service
metadata:
  name: {{ include "schema-lens.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "schema-lens.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "schema-lens.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
