apiVersion: v1
kind: Service
metadata:
  name: {{ include "solrguard.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "solrguard.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "solrguard.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
